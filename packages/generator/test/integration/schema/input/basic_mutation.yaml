mutations:
  ExampleQuery:
    graph_name: exampleQuery
    class_ref: Example
    class_name: ExampleQuery
    description: This is an example query.
    arguments:
      exampleArgument:
        type: String
        default_value: "default_value"
    fields:
      string:
        property_ref: text
      whole:
      decimal:
      flag:
      date:
      subfields:
        flatten: true
        arguments:
          exampleArgument:
            type: String
            default_value: "default_value"
        fields:
          list:
          map:
          set:
          child:
            property_ref: example
